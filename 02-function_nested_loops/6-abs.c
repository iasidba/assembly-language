#include "main.h"
/**
 *_abs -function to compute absolute value of int
 *@i:the int to be computed
 *Return:absolute value of numbers
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
